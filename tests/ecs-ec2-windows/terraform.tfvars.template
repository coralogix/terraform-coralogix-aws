aws_region       = "us-east-1"
ecs_cluster_name = "ecs-ec2-windows-testing-cluster"

# Leave empty to use default VPC subnets and default SG (plan/validate only).
# For apply, set to the subnets and security group(s) used by your Windows ECS instances.
# subnet_ids         = ["subnet-xxx", "subnet-yyy"]
# security_group_ids = ["sg-xxx"]

# Optional: Cloud Map service ARN so agent registers (agent.otel.local).
# service_discovery_registry_arn = "arn:aws:servicediscovery:REGION:ACCOUNT:service/srv-xxxx"

default_subsystem_name = "ecs-ec2-windows-tf-base_vars-test"

image         = "coralogixrepo/coralogix-otel-collector"
image_version = "v0.5.10-windowsserver-2022"

coralogix_region = "EU1"
api_key         = "cxtp_CoralogixSendYourDataApiKey"
